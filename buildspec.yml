version: 0.1
phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - npm run build
      - cd dist
artifacts:
  type: zip
  files: 
    - '**/*'
  base-directory: 'dist'
  discard-paths: yes
